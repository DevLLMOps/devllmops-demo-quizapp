# Production environment
# Usage: make prod

services:
  quizapp:
    extends:
      file: compose.base.yml
      service: quizapp-base
    build:
      context: .
      dockerfile: app/Dockerfile
      # OCPA-R1: Fixed version target
      target: prod
    # OCPA-R14: Restart policy (always for production critical service)
    restart: always
